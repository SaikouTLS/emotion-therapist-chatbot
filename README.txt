1) download SQLite studio
2) go to cmd by pressing win+R and typing cmd
	- type in pip install -r Drive:/path/to/Project/project/requirements.txt
2.5) you can also run this command in visual studio or whatever terminal in your specified IDE 
	- pip install -r requirements.txt
